# Calyxa — Claude Code Working Instructions

## Architecture decisions (locked)
- Manifest V3 only. Never suggest MV2 patterns.
- Overlay uses shadow DOM. Never mutate the host page DOM.
- All API keys are server-side. Never put keys in the extension bundle.
- Audio is never persisted. STT is real-time streaming only.
- Free tier limits are enforced server-side. Client is a hint only.
- RLS policies must exist before any table receives data.

## Stack (locked)
- Extension: WXT + React + TypeScript
- Backend: Next.js API routes on Vercel
- Database: Supabase (Postgres + Auth + RLS)
- AI: OpenAI GPT-4o-mini (server-side proxy only) — the default tutor + study-kit provider per ADR-052 (2026-07-17); Anthropic Claude Haiku retained in-tree as the TUTOR_PROVIDER=anthropic backup/eval path
- STT: OpenAI Whisper API
- TTS: ElevenLabs streaming API

## Current phase
Phase 2, Sprint 23 — Stripe billing + Pro entitlements (GA / monetization gate) — **COMPLETE (2026-07-18, all 9 tasks). Task 9 accepted in Stripe test mode: a live checkout flipped a real user to Pro via the webhook (verified in `users` + `stripe_events`), a webhook redelivery proved idempotent (`{received:true,duplicate:true}`, no double-apply), and cancel→downgrade returned the user to free (`tier=free/canceled`). Task-9 root-cause fix: no webhook endpoint existed in the Stripe sandbox — created + signing secret wired into Vercel prod. Accepted carry-forwards (Darcy's call 2026-07-18, NOT blockers): step 4 (`invoice.payment_failed`→`past_due` grace→expiry downgrade) + step 6 (dropped-webhook reconcile self-heal) were not exercised LIVE but are mutation-verified/unit-tested in Task 8. GO-LIVE for real income still needs the Stripe account activated for live payments + live-mode keys/price/webhook in Vercel — config, not code.)** Turns Calyxa from a free beta into a monetizable product. Stripe Checkout from the dashboard marks a user Pro; four idempotent webhooks (`checkout.session.completed` / `customer.subscription.updated` / `customer.subscription.deleted` / `invoice.payment_failed`, keyed on the Stripe event id in a new deny-all `stripe_events` table — insert-on-conflict-do-nothing = process-exactly-once) are the source of truth for the `users` billing columns; a daily `stripe-reconcile` cron (Sprint 16 stub → real) pulls live Stripe state for stale/expired rows so a dropped webhook self-heals within a cycle. An entitlements resolver (`resolveEntitlements(tier, status)` → the PLAN §2.8 flag set) is resolved server-side, attached to `/api/session/start` for display, and **re-checked on every Pro path** via `assertEntitlement` (the cached client value is a hint, never an authorization); `past_due` retains Pro through a grace window, only terminal `deleted`/grace-expiry downgrades to free. Pro **already** lifts the session cap via the existing `start_session` `subscription_tier = 'free'` exemption — no RPC/gate change; the webhook route reads the RAW body (`request.text()`) + `constructEvent` for signature verification, is public-but-signature-authenticated (bad sig → 400, no processing); all Stripe secrets server-only (the extension links out to `/billing`, never imports the SDK — ADR-006). ADRs **050 (Stripe billing) + 051 (entitlements resolver)** are the true next-free pair (latest ADR on disk was 049). **Migration drift:** the plan's `stripe_events` = `0021` is stale — `0020_mastery_snapshot` (Sprint 22) + `0021_study_artifact` (Sprint 21) already exist, so next-free is **`0022_stripe_events.sql`** (confirm at Task 2). `stripe_events` is processing bookkeeping, NOT personal data → deliberately **no FK-to-users and NOT on the export/erasure lists** (recorded in the migration comment so the ADR-035 invariant isn't misapplied). **Sequencing:** Sprint 19 (beta distribution) is COMPLETE (all 9 tasks committed; only Darcy-only manual CWS steps remain — the Unlisted zip upload + a human beta run, neither code); this GA/monetization sprint follows it cleanly. Handoff: the Sprint 19 `/privacy` + data-safety disclosure (ADR-046) must add Stripe customer/billing data as a processed type (Stripe = processor) before GA billing reaches real users. Phase 2, Sprint 21 — Study-materials generator (notes · practice problems · flashcards) — **COMPLETE (2026-07-14, all 8 tasks; ADR-049, migration 0021_study_artifact)**. A cost-guarded forced-tool Claude call (STUDY_KIT_TOOL, sibling of runSessionStartTool, deterministic empty-kit fallback) turns a completed session into persisted notes/practice-problems-with-solutions/flashcards, grounded in buildSessionRecap's read; RLS-scoped `study_artifact` (FK-cascade to users, session_id on delete set null), on the export list + erasure cascade; rendered in the extension recap card's "Generated for you" seat (notes/problems-with-reveal/flip-flashcards, degrades to placeholder on failure); available to all beta users, Pro-gating deferred to Sprint 23. Task-8 acceptance verified a real grounded kit live; the completion→recap-card render path is covered by a new full-Overlay integration test; a Task-8 live-find (an invalid concept key in STUDY_KIT_TOOL's input_examples that 400'd every real call) was fixed + regression-guarded. Accepted carry-forwards (NOT blockers): the in-Chrome eyeball on a full solved session (a "no card" only occurs on a 0-gradable session, by design); the full `turbo test` green also needs the parallel Sprint-22 track to regenerate its `@calyxa/ui` chart-token snapshot. **Ship gate (handoff):** the Sprint 19 disclosure + `/privacy` (ADR-046) must add "generated study materials" before this reaches the beta cohort. ⚠️ Ran out of order (a post-beta feature landed ahead of the plan's 18→19→21→22 order), filling the honestly-labelled "Generated for you" seat (ADR-031 §4 fuse resolved on the qualifier branch, ADR-040 — no marketing-copy change needed). ADR-049 (study-materials generation): a session becomes a study kit via a new forced-tool Claude call (`STUDY_KIT_TOOL`, sibling of `runSessionStartTool`, deterministic fallback), grounded in `buildSessionRecap`'s read; artifact shapes = the marketing `ArtifactKind` union (notes/problems/flashcards, problems gain worked solutions — the one superset); on-demand + cost-guarded (new `'study_kit'` `CostKind`, hard-cap refuses without a Claude call); persisted in RLS-scoped `study_artifact` (Shape 2, FK-cascade to users, `session_id on delete set null`, export + erasure covered); all beta users, Pro-gating deferred to Sprint 23. ADR resolved to **049** (not the plan's provisional 047 — 047/048 taken by Sprint 22); the study_artifact migration is **not** the plan's `0019` (taken) — next-free `0020`, contended with Sprint 22 Task 5, resolve at Task 2. Data-safety handoff: the Sprint 19 disclosure + `/privacy` (ADR-046) must add "generated study materials" before this ships to beta. Phase 2, Sprint 22 — Mastery dashboard + data charts (post-login web) — **COMPLETE (2026-07-14, all 9 tasks; ADRs 047/048, migration 0020_mastery_snapshot)**. The reserved `(dashboard)` nav slot is filled and five server-rendered, RLS-scoped, per-request-fresh views ship (Overview / Mastery / Misconceptions / Review / Activity) over `loadDashboard` (reuses `loadProfile`'s decay math), plus a forward-only `mastery_snapshot` daily cron. Chart-color tokens landed in `@calyxa/ui` (additive, AA-validated); charts are hand-built accessible CSS/SVG on the tokens (recharts@3.9.2 installed but shelved for an unresolved Bar-height defect — now effectively unused, a keep-or-remove cleanup for Sprint 23/24). Darcy's manual acceptance passed 2026-07-14. Accepted carry-forward: `build` + the dev-server test suites weren't re-run at close (a parallel `next dev` held the port-3000 one-dev-server lock); typecheck + lint + the 40 new tests are green in isolation. ⚠️ Started ahead of the plan's intended 18→19→21→22 order at Darcy's direction — Sprint 18 is COMPLETE, Sprint 19 is COMPLETE (all 9 tasks committed; only Darcy-only manual CWS steps remain), and Sprint 21 had not yet landed at that time. ADR-047 (mastery-dashboard reads: RLS-scoped, server-rendered, per-request-fresh, no cache; a dashboard-sized `loadDashboard()` reusing `loadProfile`'s decay math over the full per-user graph grouped by the 6 strands; group-by `page_url_hash` not domain; chart current state live + activity/accuracy from `session_interactions` + a forward-only `mastery_snapshot` trend, never backfilled) and ADR-048 (chart color palette as named additive AA-validated `@calyxa/ui` tokens; shadcn chart over Recharts as the one charting substrate; the `mastery_snapshot` table + daily cron reusing Sprint 16's cron infra as the forward-only trend source). ADR numbers resolved to **047/048** at execution (the plan's provisional 048/049 assumed Sprint 21 landed 047 first; it hasn't, latest on disk was 046, so 047/048 are true next-free). Task-5 migration will be `0020_mastery_snapshot.sql`. Phase 2, Sprint 19 — Store packaging + private beta distribution — **COMPLETE (all 9 tasks committed; `API_BASE` flipped to https://calyxa.app + proxy fix verified live 2026-07-15; only Darcy-only manual steps remain — the Unlisted CWS zip upload + a human fresh-machine beta run, neither code)**. ADR-045 (unlisted CWS per ADR-006; access gated by invite claim at signup, not link secrecy; `waitlist` gains additive deny-all invite columns, no FK-to-users; versioned rollback-first `wxt zip` release pipeline; server-only guarded email send with manual-batch + no-op-on-absent-credential) and ADR-046 (`/privacy` + `/terms` + Chrome data-safety disclosure generated truthfully from the verified collection surface, export/delete linked). Task-4 migration renumbered to `0019_waitlist_invite.sql` (`0018_rate_limit.sql` exists). Phase 2, Sprint 18 — Hardening: security, privacy & accessibility audit (release-candidate gate) — **COMPLETE (2026-07-12, all 9 tasks; ADR-044)**. First CI (GitHub Actions) with the audit gates: turbo + no-secret-in-bundle + extension a11y + RLS coverage sweep; security review + manifest cleaned for review (`tabs` dropped, verified live); cross-site QA matrix (4 sites, both degradation paths); Sprint-17 telemetry funnel completed (`voice_used`/`degraded_hit` wired). Accepted carry-forwards at close (not blockers): first green CI run on GitHub needs Actions secrets + a push; a live telemetry emission spot-check is still outstanding (code-complete + statically verified only); 2 filed UX enhancements (FU-1 → Sprint 24, FU-2 → Sprint 19); security-review filed items (public-endpoint rate-limiting, `API_BASE` prod flip) → Sprint 19. Sprint 17 — Onboarding + beta instrumentation — **COMPLETE (2026-07-10, all 9 tasks; ADRs 039/042/043; migration 0017 applied live)**. Sprint 16 — Cost control + compliance hardening (parallel track) — IN PROGRESS. Sprint 20's marketing landing page (parallel track) is COMPLETE (2026-07-06). Sprint 25 — landing page v2 to match the redesigned extension (parallel track, `/docs/sprint-25-plan.md`) — IN PROGRESS (Task 1 of 10 done 2026-07-09).

## File structure reference
See /docs/architecture.md

## What is NOT in scope for V1
- Firefox support
- Diagram/video understanding
- B2B / school licensing
- Parent dashboard
- Non-math subjects