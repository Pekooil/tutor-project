# Dependencies
node_modules/

# Build output
dist/
.output/
.wxt/
.turbo/
.next/
*.tsbuildinfo

# Release artifacts — versioned Chrome Web Store zips (Sprint 19, ADR-045).
# Kept addressable on disk (last N) for rollback; never committed.
extension/release/

# Environment & secrets (but keep .env*.example templates tracked)
.env
.env.local
.env.*.local
*.local

# Python artifacts (the root cost-analysis scripts are source-only)
__pycache__/
*.py[cod]
.venv/
venv/

# Logs
*.log
npm-debug.log*

# Editor / OS
.DS_Store
.idea/
.vercel
.env*
