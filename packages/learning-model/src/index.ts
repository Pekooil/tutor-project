// Public surface of @calyxa/learning-model (pure — no server-only, no Supabase, no Anthropic).
// Task 4 adds updateKnowledgeNode, retrievability, and the shared FSRS types/constants.
export const LEARNING_MODEL_PACKAGE = '@calyxa/learning-model' as const;
